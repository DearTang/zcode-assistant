## v0.7.0 - 2026-08-28

### ✨ 新增

- 新增侧边栏「ZCode 设置」页（调参 ZCode 本体行为），首个能力「模型调用重试」：
  通过 ZCode 官方 `ZCODE_MODEL_RETRY_*` 用户环境变量调整接口失败重试策略——
  最大重试次数（默认 10）/ 起步延迟（2s）/ 退避倍数（×2）/ 延迟上限（60s）
  四项均可覆盖，留空跟随 ZCode 默认、支持一键恢复，编辑时实时预览重试延迟
  序列。写入后广播环境变量变更，ZCode 运行中会弹全局重启确认。注意：全局对
  所有模型统一生效（ZCode 官方暂不支持按模型 / 按错误类型区分次数）。
- 悬浮球面板支持「固定」：单击悬浮球固定面板——固定后面板常驻，鼠标移开、
  点击面板窗口外都不会再收起；面板标题旁显示「已固定」徽标，球右上角亮点
  提示，点面板右上角 ✕ 或再次单击球收起。悬停球快速查看的自动收起行为不变
  （此前面板只随鼠标 hover 存在，点击窗口外即消失，无法边看配额边干活）。

### 🛠️ 变更

- 打包提速：release 配置由 fat LTO + `codegen-units=1` 换为 thin LTO +
  `codegen-units=16`（并行 LTO / 并行代码生成）。fat LTO 每次构建都要对整个
  依赖图做串行全量重优化，是打包最大瓶颈；调整后重复打包的编译耗时大幅下降，
  二进制大小与运行性能几乎不变（<1%）。
- 依赖例行维护与安全扫描：npm 依赖在版本范围内均为最新（npm audit 0 漏洞）；
  Rust 侧 `cargo update` 升级 36 个兼容小版本，OSV/RustSec 漏洞扫描命中的
  17 项均为低危 / 信息级（Linux 编译链与停止维护的传递依赖），不影响 Windows
  产物。
