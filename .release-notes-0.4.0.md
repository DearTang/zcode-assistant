# zcode-assistant v0.4.0

## ✨ 新增

- **同步到 cc-switch**：模型管理新增反向同步，把 zcode 的自定义供应商（含模型与上下文限制）统一写入 cc-switch 数据库的 opencode 供应商组（app_type='opencode'，切换供应商时由 cc-switch 落到 opencode.json）。baseURL + apiKey 一致的条目覆盖更新、其余新增，写入前自动备份数据库；同步前弹窗预览勾选，cc-switch 正在运行时提示重启后查看。
- **导入配置「重新获取上下文」开关**：开启后本次导入按 OpenRouter 目录 / 内置规格表匹配模型真实上下文并覆盖旧默认值；未命中的模型弹窗逐个确认（预填 200000，可修改）。仅当次生效，完成后自动还原为关。
- **设置「开机自启动」开关**：登录系统自动启动 zcode-assistant 并驻留托盘（tauri-plugin-autostart）。

## 🛠️ 变更

- **应用图标重设计**：Z 字母标识（深色靛蓝+青绿渐变），全套 bundle / Windows Store / Android / iOS 图标派生更新。
- **NSIS 安装包改造**：升级时旧版静默卸载（一轮「下一步」完成升级）；卸载支持 /REBOOTOK 处理进程占用；安装包 EXE 图标 = 新 Z 字母图标。
- **⚡ 检测改为「配置验证」口径**：只验证 baseURL + apiKey 连通性，文案明确「不代表模型可用 / 额度充足」。
- **配额百分比统一取整显示**：如 46.123112 % → 46%，消除浮点精度泄漏。
- **「模型用量展示方案」升级为全局口径**：总览与供应商管理额度行跟随方案切换已用/剩余（颜色始终按已用度分级），设置页拆分为独立分区。

## 🐛 修复

- **火山方舟 AccessKey 查询不到用量**：解析 HTTP 400/403 的 ResponseMetadata.Error 信封并识别鉴权类错误码，给出账号级 AccessKey 引导；空结果附原始响应片段。
- **dev 模式「覆盖启动」弄断 tauri dev 会话**：debug 构建改为仅重载页面，不再重启进程（生产版行为不变）。
- **用量统计与 zcode 对不上**：同步时按 request_id 全量对账，回收 zcode 侧已删除的记录。
- **模型拖拽排序在 zcode 内不生效**：按新顺序写入模型的 zcode.priority 与 zcode.modified=true，zcode 重启后即按新顺序展示。
