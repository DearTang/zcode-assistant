## v0.6.0 - 2026-08-25

### ✨ 新增

- 设置新增「外观定制」（参考 ZCode 美化，仅主窗口，即时生效，可一键还原默认）：
  主题色（6 组预设色板 + 色相滑杆，深/浅主题分别适配）、透明度（面板与表面
  不透明度，调低让光晕 / 背景图透出）、背景图（选择本地图片铺满窗口底层 +
  不透明度调节，复用美化页的选图与预览读取；生效时侧边栏 / 顶栏自动降为
  轻纱玻璃，背景图连续覆盖整窗，漂移光晕同步压低）。
- 自动切换规则新增「同时切换主供应商」开关（默认关闭）：规则触发切换时，把
  「模型管理」的 ⭐ 主供应商标记同步设为目标供应商，总览 / 悬浮球 / 托盘的
  配额展示随之跟随；手动「测试」以及「已是目标供应商」的跳过场景同样生效。

### 🛠️ 变更

- 构建提速配置：`[lib] crate-type` 去掉桌面应用用不到的 `staticlib` / `cdylib`
  （Tauri 移动端遗留），MSVC 目标改用 Rust 自带 rust-lld 链接器
  （`src-tauri/.cargo/config.toml`）。对本机实测收益被环境波动（安全软件扫描 /
  系统负载）淹没，但属官方推荐形态，全量重建与磁盘占用更低。

### 🐛 修复

- 修复升级后「开机自启动」失效：NSIS 升级流程会先静默执行旧版卸载器，其
  非更新模式会顺手删除 HKCU Run 的自启注册项，新装的版本又不会自动补注册，
  设置开关随后被查询同步为关闭。双管齐下：安装模板让升级路径始终以 /UPDATE
  执行旧版卸载器（保留自启项与快捷方式）；应用启动时按偏好自愈——记录为
  开启但系统注册项丢失时重新注册，注册项指向旧安装路径时刷新。
