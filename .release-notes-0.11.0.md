## v0.11.0 - 2026-09-11

### ✨ 新增

- 模型管理新增 Oh My Pi（OMP）模型双向同步：可从 `~/.pi/agent/models.json` 预览并导入供应商、模型名称及上下文/输出限制，也可将 ZCode 自定义供应商同步回 OMP；两端均需在预览中勾选确认，按 `baseUrl + apiKey` 覆盖或新增，不删除既有条目。导出自动生成 `models.json.bak`，且不会修改 OMP `settings.json` 的默认供应商/模型。