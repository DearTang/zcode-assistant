# v0.8.0 (2026-09-02)

## ✨ 新增

- 项目管理会话列表支持全选 + 批量关闭：展开项目后，会话表头新增全选框（支持半选态），勾选会话后批量操作条出现「关闭所选」——批量归档所选会话（已归档自动跳过），在 zcode 会话列表隐藏、可随时在「查看历史会话」恢复，清理积压会话不必再逐个点击。
- 自动切换「应用启动」规则支持限定生效时间范围：编辑规则选类型「应用启动」时，可选择「全天生效」（默认）或「指定时间范围」（起止 HH:MM，默认 09:00–18:00，分钟为 00），仅当应用启动时刻落在范围内才触发；结束时间小于开始时间视为跨零点范围（如 22:00–06:00）。规则列表同步展示生效范围。

## 🐛 修复

- 修复「应用启动」自动切换被误判「已是目标」而静默跳过的问题：此前「是否已在目标模型」依据最近一次自动切换写入的内部标记判断，用户手动把模型切走（供应商不变）后该标记不更新，下次启动时被误判为已在目标、不执行切换；现改为读取最近活跃会话的实际模型选择，手动切换可被感知，启动触发恢复可靠。
