## v0.5.1 - 2026-08-24

### 🛠️ 变更

- 「应用美化」改为 app.asar 原地补丁，不再整包解包/重打包：原先对 284MB 的
  包要解出几千个小文件再全量重打包 + 双倍 SHA256（数十秒），现在只更新 asar
  JSON 头中被触及的条目、新内容追加到 payload 尾部、原字节顺序照抄，实测
  2.3 秒完成；关闭 ZCode 的停机窗口也随之缩短。

### 🐛 修复

- 修复 ZCode 升级后美化备份过期的问题：升级会整体替换 app.asar，但备份逻辑
  只看文件是否存在，导致重新应用时不会备份新版本官方包、且「还原」会把旧版本
  asar 覆盖到新版本安装上（主程序与渲染层版本错配，可能直接损坏 ZCode）。
  现在备份感知版本：升级后首次「应用美化」自动用官方包重建备份；备份版本与
  当前安装不一致时还原被拒绝并说明原因；美化状态同步显示备份所属的 ZCode
  版本，不一致时黄字提醒并按情形给出指引（未注入 → 下次应用自动重建备份；
  已注入 → 禁用「还原」按钮，提示重装/修复 ZCode）。
